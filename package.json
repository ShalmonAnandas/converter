{
  "name": "converter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.js",
    "build": "node scripts/build.js",
    "test": "node --test",
    "typecheck": "node --check public/app.js && node --check public/registry.js && node --check scripts/serve.js && node --check scripts/build.js && for f in public/lib/*.js; do node --check \"$f\" || exit 1; done",
    "check": "npm run typecheck && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.28.1",
  "description": "A local-first workbench of 92 developer tools that never uploads your data."
}
